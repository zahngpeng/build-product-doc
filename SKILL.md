---
name: build-product-docs
description: 生成、补全、更新和审核中文产品文档，包括项目方案、业务模块PRD、页面规格与验收；也用于分析业务动作的跨模块影响和事件时点。适用于产品需求与原型规格，不负责实现业务代码或编写接口、数据库设计。
---

# 产品文档架构师

以项目事实和已确认需求为依据，交付能指导原型设计与验收的中文文档。用户指定的范围、术语和已有文档约定优先于本 Skill 的默认结构。

## 确定本次工作

读取适用的 AGENTS.md、相关文档与原型资料，用 rg 定位本次范围及引用。判断模式、读者、交付范围和事实缺口，再选择模板。

| 模式 | 输入 | 工作与交付 |
|---|---|---|
| 新建／补全 | 已确认范围、项目资料、现有缺口 | 只补足实际需要的文档；先定义公共事实，再建立页面与验收引用 |
| 更新 | 已确认变更、现有定义与引用 | 列出影响项，修改权威定义及相关引用，检查同类遗漏与废弃规则残留，说明变更和验证结果 |
| 审核 | 待审核文档及依据 | 输出问题位置、证据、影响、建议和待确认项；区分已验证缺陷与推断，修改须在用户授权范围内 |

单个页面、模块或动作的请求只处理必要上下文。沿用已确认的方案；项目要求先确认且当前范围尚未确认时，先给可审核的范围与方案。不要因本 Skill 的默认目录要求重新确认已决定的事项。

## 事实与决策

- 用户明确确认的新规则作为本次依据；冲突的旧文档列入同步范围。
- 已确认 PRD 表达预期行为，代码表达当前实现。发现差异时说明双方依据，不能仅凭实现覆盖需求。
- Mock、截图与示例数据只证明观察到的表现，不能单独推出业务规则、真实账号或正式数据。
- 无法证明的结论保留为待确认，写明影响及是否阻塞本次交付。金额算法、权限、归属、生效时点或逆向处理存在关键歧义时，先用具体场景说明已知事实、前后变化及建议，再确认关键选择。用户要求逐项确认时，一次讨论一个业务决定；未确认的建议不写成既定规则，其他独立工作继续。
- 已排除的能力写“不适用”或“本期不支持”及原因。模板中的候选能力不构成需求。
- 在现有来源列或 sources 中记录可定位的文件章节、用户确认内容或原型版本，无需另建来源登记目录。

## 按需读取规范

- 新建、扩展或评估目录组织时，读取 [产品文档结构规范](references/产品文档结构规范.md)。先区分独立业务身份和内部权限角色；已有结构仅在明确授权迁移时改变。
- 编写或审核具体内容时，读取 [产品文档内容规范](references/产品文档内容规范.md) 的通用填写约定、阅读与表达及对应文档类型章节。
- 分析动作结果、审核业务逻辑完整性／闭环，或变更会影响上下游结果时，额外读取 [业务动作全链路影响建模规范](references/业务动作全链路影响建模规范.md)，在本次范围内串联实际动作。普通文字或排版调整不扩展为全链路审核。
- 运行脚本或判断检查覆盖面时，读取 [脚本与校验约定](references/脚本与校验约定.md)。

## 定义与引用

业务模块是页面与验收的父级。默认交付项目级文档、端口视图、模块公共文档、实际页面和相应验收；需要独立业务旅程时再增加身份文档。模板位于 assets/模板，按需选取，不以填满模板为完成条件。

| 事实 | 权威位置 | 其他文档的用法 |
|---|---|---|
| 项目范围、模块边界、跨模块原则 | 项目级文档 | 引用与当前任务相关的部分 |
| 模块规则、业务对象与内部权限原则 | 模块主PRD | 使用规则ID；身份文档只定义确有依据的差异 |
| 流程、状态定义及事件时点 | 业务流程与状态流转 | 页面引用状态；加载、网络错误、超时等操作反馈不直接成为业务状态 |
| 字段含义、取值、计算与跨页面使用总览 | 字段字典 | 保留产品字段矩阵；页面引用字段与规则ID，描述顺序、控件和呈现 |
| 页面交互与二次交互 | 触发页面PRD | 验收引用页面规格，不新增业务要求 |
| 验收用例 | 适用范围的验收PRD | 页面可引用用例或保留验收摘要，不复制执行结论 |

更新时复用现有稳定 ID；重排文档或页面不重新编号。按“变更事实 → 权威定义 → 页面／端口／身份引用 → 验收”检查影响，保留未涉及的用户内容。确需重复展示的摘要注明来源，并核对一致性。

## 交付与验证

脚本只检查已声明的结构与引用，业务语义仍由文档审核确认。选择与交付范围一致的校验方式；报告实际错误、警告和未检查部分，不能把“0 错误”等同于需求已确认或验收通过。

交付前分别核对结构、业务闭环和可读性：本次要求和变更均有着落；字段、页面、权限、状态、业务时点和验收互相一致；实际弹窗与抽屉有完整交互；读者能定位入口、理解操作结果和后续处理；阻塞项已解决或明确交付为草稿。说明改了什么、检查了什么及仍待确认的内容。
