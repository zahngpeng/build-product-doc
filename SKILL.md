---
name: build-product-docs
description: 生成、补全、更新和审核软件项目的结构化中文产品文档。适用于需要输出项目级产品文档、端口产品视图和业务模块PRD的项目；端口产品视图按PC、小程序、APP等端口划分功能清单和核心业务旅程，每个业务模块PRD包含公共主文档，以及按身份划分的模块主PRD、模块功能清单、列表页PRD、详情页PRD、其他页面PRD和身份模块验收PRD。
---

# 产品文档架构师

## 工作目标

根据当前项目已有资料，用中文生成结构清晰、层级明确、可直接指导高保真原型设计的产品文档。

严格区分：

- 项目级公共内容；
- 端口产品视图；
- 业务模块PRD中的公共主文档；
- 同一业务模块下不同身份的模块文档、页面、字段、操作和验收差异。

将业务模块作为身份文档、页面PRD和验收PRD的唯一父级。不得把按身份文档、页面PRD或验收PRD提升为与业务模块并列的项目级目录。

## 允许输出的文档

只生成以下文档。

### 1. 项目级产品文档

1. 项目总体方案；
2. 产品总PRD；
3. 项目范围与版本规划；
4. 产品功能架构；
5. 信息架构与导航结构。

### 2. 端口产品视图

每个实际产品端口分别生成：

1. 端口功能清单，按照业务模块分组，并标明每项功能的适用身份；
2. 端口核心业务旅程，标明各步骤的执行身份以及跨端口、跨身份衔接。

### 3. 业务模块PRD

每个业务模块建立一个独立目录，目录内包含以下两层。

#### 3.1 业务模块公共主文档

1. 模块主PRD；
2. 模块功能清单；
3. 业务流程与状态流转；
4. 字段字典。

#### 3.2 按身份划分的模块文档

每个模块下，按照实际使用身份分别生成：

1. 身份模块主PRD；
2. 身份模块功能清单；
3. 列表页PRD；
4. 详情页PRD；
5. 新增、编辑、审核、工作台等其他实际页面PRD；
6. 身份模块验收PRD。

页面PRD必须包含该页面内的弹窗、抽屉、确认框、气泡、子表单等二次交互，包括字段、默认值、必填、校验、联动、按钮、权限、成功结果、失败结果和关闭规则。

例如，订单管理模块先生成订单管理公共主文档；再在该模块下分别建立采购商、供应商身份目录。采购商目录保存采购商订单管理模块主PRD、功能清单、列表页PRD、详情页PRD、其他实际页面PRD和采购商订单管理验收PRD；供应商目录按相同结构独立保存供应商版本。两个身份不得共用一份页面PRD或验收PRD。

除非用户另外明确要求，不生成会议结论、需求原子池、来源登记、项目画像、模板选择记录、Mock数据文档、项目综合验收、模块综合验收、接口文档、数据库文档或开发方案。

## 开始前检查

1. 读取当前项目中的 `AGENTS.md`、`DESIGN.md`、现有PRD、原型说明和其他直接相关资料。
2. 使用 `rg --files` 检查已有文档，避免重复创建和覆盖用户内容。
3. 明确项目名称、角色、端口、业务模块和本次生成范围。
4. 信息不足但不影响结构时，使用 `[待确认]`；信息不足且会改变文档结构时，只询问一个关键问题。
5. 全部正文、标题、表格字段、状态说明和验收内容使用中文。

## 必须读取的规范

- 初始化或调整文档结构前，读取 [references/产品文档结构规范.md](references/产品文档结构规范.md)。
- 生成或更新任何正式文档前，读取 [references/产品文档内容规范.md](references/产品文档内容规范.md)。

## 生成顺序

按照以下顺序生成，后层引用前层，不重复定义：

1. 项目级产品文档；
2. 端口产品视图；
3. 逐个建立业务模块PRD；
   1. 先生成该模块的公共主文档；
   2. 再在该模块下建立各身份目录；
   3. 在每个身份目录生成身份模块主PRD和功能清单；
   4. 在该身份目录下生成各端口的列表页、详情页和其他页面PRD；
   5. 最后生成该身份的模块验收PRD。

如果用户只要求某个模块或某个页面，只读取并生成该范围必需的上层文档，不强制生成全项目文档。

## 内容归属规则

- 项目目标、整体范围、模块关系放在项目级文档。
- 产品总PRD包含产品、角色、端口、业务模块、业务对象和页面关系的信息结构总览；详细导航与页面层级放在《信息架构与导航结构》。
- 某端口包含哪些业务模块、功能、页面入口和核心业务旅程，放在端口产品视图。
- 端口功能清单以端口为父级、以业务模块为分组，并在功能项中标明适用身份；不得按身份重复生成端口功能清单。
- 模块公共目标、公共流程、公共状态和字段含义，放在业务模块主文档。
- 某身份的数据范围、功能、状态文案和操作权限，放在该业务模块下对应身份的模块文档。
- 某身份的查询条件、展示字段、按钮和页面交互，放在该业务模块、该身份、该端口下的页面PRD。
- PC、小程序、APP等端口的布局与交互差异，放在对应页面PRD。
- 字段字典按头部字段、明细字段以及状态、金额、审计和派生字段分组，统一记录字段来源、取值、必填性及新增页、编辑页、列表、筛选、详情的使用总览。
- 页面PRD通过字段ID引用字段字典，并进一步描述页面顺序、布局、交互、状态、权限和身份差异。
- 查询条件属于列表页PRD。
- 弹窗、抽屉、确认框、气泡和子表单属于触发它的页面PRD；只有用户明确要求时才单独成文档。
- 验收PRD放在对应业务模块的对应身份目录，只验证该身份已有的模块和页面需求，不增加新需求。

## 字段字典要求

字段字典主表固定使用：字段ID、字段名称、字段来源、取值说明、必填性、新增页、编辑页、列表展示、可筛选、详情展示、字段说明、备注。不得用纯数据库字段表替代产品字段矩阵。

同时补充枚举与选项、字段联动与计算、校验规则、身份与端口差异。字段业务含义和跨页面使用总览以字段字典为准，具体页面表现以对应身份和端口的页面PRD为准。

## 页面PRD要求

### 列表页

至少包含：

- 页面目标、角色、端口和数据范围；
- 所属模块、路由、页面地址及对应原型版本；
- 页面结构与状态页签；
- 查询条件、占位提示、默认值、清空规则、查询时机、匹配方式、重置规则和字段联动；
- 列表字段、顺序、格式、排序、筛选、脱敏和显示条件；
- 分页或加载方式、查询条件保留和刷新规则；
- 顶部操作、行内操作和批量操作；
- 导出、打印和下载规则；
- 页面跳转；
- 空、加载、错误、无权限和禁用状态；
- 弹窗、抽屉及其字段；
- 验收标准。

### 详情页

至少包含：

- 页面目标、角色、端口和数据范围；
- 所属模块、路由、页面地址及对应原型版本；
- 页面头部、业务状态和信息分组；
- 展示字段、顺序、格式、脱敏、空值和显示条件；
- 明细、子表、关联信息、时间线和操作记录；
- 不同状态和权限下的操作按钮；
- 打印、导出和下载规则；
- 页面跳转；
- 数据不存在、失效、错误和无权限状态；
- 弹窗、抽屉及其字段；
- 验收标准。

### 其他页面

根据页面类型描述路由、原型版本、表单字段、默认值、必填、校验、字段联动、按钮、保存、提交、取消、离开页面、重复提交、页面状态和二次交互。

## 初始化文档目录

项目尚未建立文档结构时，可运行：

```bash
python3 <skill目录>/scripts/scaffold_product_docs.py \
  --project-root <项目目录> \
  --project-name "<项目名称>" \
  --role "<角色一>" \
  --role "<角色二>" \
  --module "<模块一>" \
  --module "<模块二>" \
  --endpoint "<角色一>=<端口一>,<端口二>"
```

只有已经确认各模块均存在列表页和详情页时，才增加 `--include-core-pages`。脚本默认跳过已有文件，不覆盖用户文档。

## 模板使用

- `assets/模板/项目级产品文档/`：5类项目级产品文档；
- `assets/模板/端口产品视图/`：端口功能清单、端口核心业务旅程；
- `assets/模板/业务模块主文档/`：4类业务模块主文档；
- `assets/模板/按身份模块文档/`：按身份划分的模块主PRD和功能清单；
- `assets/模板/页面PRD/`：列表页、详情页和其他页面PRD；
- `assets/模板/验收文档/`：身份模块验收PRD。

只复制实际需要的模板，不创建空白的额外文档。

## 校验

生成或更新后运行：

```bash
python3 <skill目录>/scripts/validate_product_docs.py \
  --project-root <项目目录>
```

交付前确认：

- 文档只包含规定范围；
- 项目、端口、模块、身份和页面层级正确；
- 端口产品视图按照端口划分，功能清单按照业务模块分组并标明适用身份；
- 公共规则没有在多个身份文档中重复定义；
- 不同身份的查询、字段、按钮、权限和数据范围分别明确；
- 功能能够追踪到流程、状态、字段、页面和验收；
- 业务流程与状态流转文档同时完整覆盖流程步骤和状态机定义；
- 流程包含时限、通知、补偿和操作记录；
- 状态包含自动流转、并发、幂等及重复提交处理；
- 页面中的所有弹窗、抽屉和子表单均有字段及交互说明；
- 页面明确路由、原型版本、子表、刷新以及导出打印规则；
- 每个身份模块均有对应验收PRD；
- 验收覆盖业务边界、并发、通知、操作记录及需求覆盖关系；
- 现有用户文档未被覆盖。
