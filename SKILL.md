---
name: build-product-docs
description: 生成、补全、更新和审核软件项目的结构化中文产品文档。适用于需要输出项目级产品文档、端口产品视图和业务模块PRD的项目；端口产品视图按PC、小程序、APP等端口划分功能清单和核心业务旅程，每个业务模块PRD包含公共主文档，以及按身份划分的模块主PRD、模块功能清单、列表页PRD、详情页PRD、其他页面PRD和身份模块验收PRD。
---

# 产品文档架构师

## 工作目标

根据项目已有资料，用中文生成结构清晰、层级明确、可直接指导高保真原型设计的产品文档。

始终区分：

- 项目级公共内容；
- 端口产品视图；
- 业务模块公共主文档；
- 同一业务模块下不同身份的模块文档、页面、字段、操作和验收差异。

将业务模块作为按身份模块文档、页面PRD和身份模块验收PRD的唯一父级。不得将这些文档提升为与业务模块并列的项目级目录。

## 允许输出的文档

只生成以下文档：

1. 项目级产品文档：项目总体方案、产品总PRD、项目范围与版本规划、产品功能架构、信息架构与导航结构；
2. 端口产品视图：每个实际端口的端口功能清单和端口核心业务旅程；
3. 业务模块公共主文档：模块主PRD、模块功能清单、业务流程与状态流转、字段字典；
4. 按身份模块文档：身份模块主PRD、身份模块功能清单；
5. 身份页面PRD：列表页、详情页以及新增、编辑、审核、工作台等实际页面；
6. 身份模块验收PRD。

除非用户明确要求，不生成会议结论、需求原子池、来源登记、项目画像、模板选择记录、Mock数据文档、项目综合验收、模块综合验收、接口文档、数据库文档或开发方案。

## 执行流程

### 1. 检查现状

1. 读取项目中的 `AGENTS.md`、`DESIGN.md`、现有PRD、原型说明和直接相关资料。
2. 使用 `rg --files` 检查已有文档，避免重复创建或覆盖用户内容。
3. 明确项目名称、身份、端口、业务模块和本次处理范围。
4. 信息不足但不影响结构时标记 `[待确认]`；信息不足且会改变文档结构时，只询问一个关键问题。
5. 全部正文、标题、表格字段、状态说明和验收内容使用中文。

完成条件：已确认本次范围及其必需的上层关系，并识别所有现有文档。

### 2. 按需读取规范

始终读取：

- [01_产品文档结构规范.md](references/01_产品文档结构规范.md)

仅按本次任务涉及的文档类型继续读取：

- 项目级文档：[02_项目级产品文档规范.md](references/02_项目级产品文档规范.md)
- 端口产品视图：[03_端口产品视图规范.md](references/03_端口产品视图规范.md)
- 业务模块公共主文档：[04_业务模块公共主文档规范.md](references/04_业务模块公共主文档规范.md)
- 按身份模块文档：[05_按身份模块文档规范.md](references/05_按身份模块文档规范.md)
- 列表页、详情页和其他页面：[06_页面PRD规范.md](references/06_页面PRD规范.md)
- 字段字典或页面字段规则：[07_字段字典规范.md](references/07_字段字典规范.md)
- 身份模块验收：[08_身份模块验收PRD规范.md](references/08_身份模块验收PRD规范.md)

不得为单一页面任务加载全部内容规范。完成条件：本次任务的每种文档都有且只有对应规范作为内容依据。

### 3. 生成或更新文档

全量生成时按以下顺序执行，后层引用前层，不重复定义：

1. 生成项目级产品文档；
2. 按实际端口生成端口产品视图；
3. 逐个建立业务模块；
4. 先生成业务模块公共主文档；
5. 再在业务模块下建立各身份目录；
6. 生成身份模块主PRD和身份模块功能清单；
7. 在对应身份和端口下生成实际存在的页面PRD；
8. 最后生成该身份的模块验收PRD。

用户只要求某个模块、身份或页面时，只读取并生成该范围必需的上层关系，不强制补齐全项目文档。

完成条件：每份文档位于正确父级，内容只覆盖本次范围，并能追踪到对应上层定义。

### 4. 遵守内容归属

- 将项目目标、整体范围和模块关系放在项目级文档。
- 将产品、身份、端口、业务模块、业务对象和页面关系的总览放在产品总PRD；将详细导航和页面层级放在信息架构与导航结构。
- 将“谁在什么时间或触发节点做了什么事，并产生什么结果”按业务发生顺序写入业务故事版。产品总PRD描述跨模块完整故事，模块主PRD描述模块内部故事；使用连续编号的完整句子，不得写成“作为……，我希望……”格式或功能点清单。
- 将端口包含的模块、功能、页面入口和核心业务旅程放在端口产品视图。
- 以端口为端口功能清单的父级，以业务模块分组，并在功能项中标明适用身份；不得按身份重复建立端口功能清单。
- 将模块公共目标、流程、状态、规则和字段含义放在业务模块公共主文档。
- 将某身份的数据范围、功能、状态文案和操作权限放在该业务模块下对应身份的模块文档。
- 将某身份、某端口的查询、字段、按钮、页面状态和交互放在对应页面PRD。
- 将查询条件放在列表页PRD。
- 将弹窗、抽屉、确认框、气泡和子表单放在触发它的页面PRD；只有用户明确要求且其具备独立业务流程时才单独成文档。
- 让不同身份分别维护页面PRD和验收PRD，不得共用。
- 让验收PRD只验证已有需求，不得增加新需求。

## 初始化目录

项目尚未建立产品文档结构时，运行：

```bash
python3 <skill目录>/scripts/scaffold_product_docs.py \
  --project-root <项目目录> \
  --project-name "<项目名称>" \
  --role "<身份一>" \
  --role "<身份二>" \
  --module "<模块一>" \
  --module "<模块二>" \
  --endpoint "<身份一>=<端口一>,<端口二>"
```

只有已经确认各模块均存在列表页和详情页时，才增加 `--include-core-pages`。脚本默认跳过已有文件，不覆盖用户文档。

从 `assets/模板/` 只复制实际需要的模板，不创建空白的额外文档。

## 校验

生成或更新后，按本次范围运行：

```bash
python3 <skill目录>/scripts/validate_product_docs.py \
  --project-root <项目目录> \
  --scope <all|project|endpoint|module-common|identity|page>
```

全量项目使用 `all`；局部任务选择对应范围。需要将警告视为失败时增加 `--strict`。

交付前确认：

- 文档只包含规定范围；
- 项目、端口、模块、身份和页面层级正确；
- 公共规则未在多个身份文档中重复定义；
- 不同身份的查询、字段、按钮、权限和数据范围分别明确；
- 功能可以追踪到流程、状态、字段、页面和验收；
- 页面内所有二次交互都有字段和交互说明；
- 每个实际身份模块都有对应验收PRD；
- 现有用户文档未被覆盖；
- 对应范围校验没有错误。
